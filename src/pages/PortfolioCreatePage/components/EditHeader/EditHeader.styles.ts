import styled from '@emotion/styled';

export const PublishHeader = styled.div`
  position: fixed;
  top: 36px;
  z-index: 10;

  display: flex;
  gap: 18px;
  align-items: center;

  svg {
    cursor: pointer;
    width: 25px;
    height: 25px;
  }
`;
